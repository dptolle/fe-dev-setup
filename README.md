# Setup for Frontend projects

Uses eslint and prettier (automatically executed upon commit using husky) to keep the code nice and pretty.

Uses a webpack development server for development:

> npm start
